{{> nav/nav-block}}

<div id="main-container" class="container">
  <div class="jumbotron text-center header-wrap" align="center">

    <p class="lead">Welcome to <strong>Munchies</strong></p>

    <hr class="my-4">
    <div class="log">
      {{#if isloggedin}}
        You are logged in as {{user.firstName}}
        <br>
        <br>
        <a href="/logout"> <button type="btn" class="btn">Logout</button></a>
      {{else}}

        <p style="color: blue;">You need to log in to use this app.</p>

      {{/if}}
      <h5 id="login-err-msg"></h5>
    </div>
  </div>
</div>

{{> modal/login-block}}
